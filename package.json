{
  "name": "summoner-atlas",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --filter @summoner-atlas/web dev",
    "build": "pnpm --filter @summoner-atlas/web build"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
